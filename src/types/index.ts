// id: item.id,
// 				class: item.className,
// 				tag: item.tagName,
// 				innerText: item.innerText,
// 				href: item.href,
export type Trt = {
	class: string;
	tag: string;
	innerText: string;
	href: string;
	id: number;
};
